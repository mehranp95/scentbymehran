# AGENTS.md — ScentbyMehran

این ریپو دانش کانال تلگرام **عطر با مهران** (`@Scentbymehran`) است.

## وقتی کاربر ازت می‌خواهد ریویو / کپشن / متن عطر بنویسی

1. اول `docs/style-guide.md` را بخوان (حداقل بخش‌های ۱ تا ۷ و چک‌لیست ۱۰).
2. قواعد فشرده را از `docs/style-rules.json` رعایت کن.
3. در صورت نیاز، نمونه‌های واقعی را از `corpus/reviews.txt` مرور کن.
4. پیش‌فرض: **رجیستر A** (ریویوی روایی/اسه‌ای)، مگر کاربر صریحاً تست لحظه‌ای روی پوست بخواهد (رجیستر B).
5. قبل از تحویل نهایی، چک‌لیست ۱۲بندی انتهای `docs/style-guide.md` را روی متن اعمال کن.

## هویت نویسنده (خلاصه)

- محاوره‌ای، گرم، خودی؛ نه متخصص صنعتی، نه لوس.
- قلاب زندگی/حال روحی که با مفهوم مرکزی عطر هم‌معناست.
- حداکثر ۲–۵ نوت؛ هر نوت با معنا؛ بدون هرم نوت رسمی.
- همیشه یک «هنرِ کار» شناسایی شود.
- پرفورمنس کوتاه و کیفی؛ ضعف ماندگاری با «رسالت» توجیه می‌شود.
- موقعیت مصرف + رنگ/استایل لباس تقریباً همیشه هست.
- واژگان امضا: آچار فرانسه، استوار، طراوت، رسالت، رفرنس، پرفورمنس، اثر هنری، خلوت، تعادل/افراط و تفریط، بک گراند، دیزاین.
- بدون هایپ، امتیاز عددی، تبلیغ فروش، کلون‌بازی، شیمی‌بافی، CTA کلیشه‌ای.

## خارج از اسکوپ

- لاگین به اکانت‌های فروشگاه عطر کاربر یا استفاده از پسوردش.
- انتشار خودکار در تلگرام بدون درخواست صریح.
